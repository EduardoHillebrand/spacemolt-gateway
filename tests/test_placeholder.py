# Placeholder so pytest finds the tests/ directory and exits 0.
# Remove this file when real tests are added.


def test_placeholder() -> None:
    """Ensures pytest can collect and run at least one test."""
    assert True
